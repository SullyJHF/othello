# Local Development Environment Configuration
# Copy this file to .env.local for local Docker testing

# Host configuration - set this to your Docker host IP
# For most setups, use localhost. For corporate networks/WSL, you may need your machine's IP
LOCAL_HOST=localhost

# Node.js server configuration
NODE_ENV=production
PORT=3000
ROOT_DIR=/app
HOST=localhost

# Port configuration
APP_PORT=3000

# Optional: Custom container name
CONTAINER_NAME=othello-local

# Optional: Resource limits (uncomment to enable)
# MEMORY_LIMIT=1g
# CPU_LIMIT=1.0

# Proxy settings for VPN/corporate environments (uncomment if needed)
# HTTP_PROXY=http://proxy.company.com:8080
# HTTPS_PROXY=http://proxy.company.com:8080
# NO_PROXY=localhost,127.0.0.1